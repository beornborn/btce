#select_indicator.col-xs-3
  %form
    - default ||= Indicator.the_best_ichimoku.id
    = select_tag 'indicator_id', options_from_collection_for_select(Indicator.all, :id, :name, default), class: 'form-control'
